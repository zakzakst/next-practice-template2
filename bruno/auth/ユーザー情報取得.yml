info:
  name: ユーザー情報取得
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
